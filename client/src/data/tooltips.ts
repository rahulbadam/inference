import type { TooltipContent } from "../types";

export const EXTENDED_TOOLTIPS: Record<string, TooltipContent> = {
  "ttft": {
    term: "TTFT (Time To First Token)",
    shortDescription: "The latency from receiving a prompt to generating the first output token.",
    detailedExplanation: "TTFT measures the prefill phase where the model processes the entire input prompt to compute the initial KV cache and first output token. This involves a full forward pass through all layers and is compute-bound (limited by GPU FLOPS). Longer prompts, larger models, and lower batch sizes increase TTFT. Optimizations like prefix caching can reduce TTFT for repeated prompts.",
    example: "A 70B model on H100 has ~50ms TTFT for 512 tokens, but ~800ms for 8K tokens.",
    formula: "TTFT ≈ 2 × parameters × prompt_tokens / (GPU_FLOPS × utilization)",
  },
  "tokens-per-second": {
    term: "Tokens Per Second",
    shortDescription: "How many tokens the model can generate each second during decoding.",
    detailedExplanation: "This is the autoregressive generation speed — each new token requires loading all model weights from VRAM. It's typically memory-bandwidth-bound, not compute-bound. Larger batches improve throughput but not single-stream tokens/sec. Quantization, faster GPUs, and speculative decoding increase this.",
    example: "A 7B model on RTX 4090 achieves ~100 tok/s at FP16, ~220 tok/s at INT4.",
    formula: "tok/s = memory_bandwidth / (2 × params × bytes_per_param)",
  },
  "throughput": {
    term: "Throughput",
    shortDescription: "Total tokens processed per second across all concurrent requests.",
    detailedExplanation: "Throughput = tokens_per_second × effective_concurrency. This is the aggregate measure of how much work the system handles. Batching is the primary lever for increasing throughput — serving 32 requests together yields ~20x the throughput of serving them serially, with only ~2x latency increase per request.",
    example: "A single H100 serving Llama-70B with batch=32 achieves ~3,200 tok/s throughput.",
    formula: "Throughput = batch_size × tokens_per_second",
  },
  "gpu-utilization": {
    term: "GPU Utilization",
    shortDescription: "Percentage of GPU compute units actively processing inference workloads.",
    detailedExplanation: "Low utilization (<40%) means the GPU is starved of work — either batch size is too small, or the workload is memory-bound with compute units idle while waiting for data. High utilization (>90%) means the GPU is saturated. For inference, ideal utilization varies: 60-80% is typical for memory-bound workloads, while compute-bound kernels can hit 95%+.",
    example: "Batch size 1 on a 70B model typically yields 15-30% GPU utilization.",
    formula: "Utilization = active_SMs / total_SMs × 100%",
  },
  "vram": {
    term: "VRAM (Video RAM)",
    shortDescription: "GPU memory used for model weights, KV cache, activations, and runtime overhead.",
    detailedExplanation: "VRAM is the most constrained resource in LLM inference. Model weights consume the bulk (e.g., 70B FP16 = 140GB). The KV cache grows with batch size and sequence length. Activations are temporary per-layer tensors. Additional overhead comes from CUDA contexts, cuBLAS workspaces, and memory fragmentation. Exceeding VRAM causes OOM crashes or CPU offloading (very slow).",
    example: "Running Llama-70B FP16 requires ~160GB VRAM with batch=1, context=8K.",
    formula: "VRAM = weights + KV_cache + activations + overhead",
  },
  "kv-cache": {
    term: "KV Cache",
    shortDescription: "Key-Value cache stores intermediate attention computations to avoid recomputation during autoregressive generation.",
    detailedExplanation: "During transformer inference, each generated token needs to attend to all previous tokens. Instead of recomputing key and value vectors for all previous tokens at every step, the KV cache stores these values in GPU memory. This dramatically speeds up generation but consumes significant VRAM proportional to batch size × sequence length × hidden dimension × layers × 2 (keys and values) × precision bytes. For long contexts, the KV cache can exceed the model weights in memory consumption.",
    example: "A 70B model with 8192 context and batch size 32 can use 200+ GB of KV cache memory alone.",
    formula: "KV Cache (GB) = 2 × layers × num_kv_heads × head_dim × seq_len × batch_size × bytes_per_element / 1e9",
  },
  "batch-size": {
    term: "Batch Size",
    shortDescription: "Number of requests processed together in a single forward pass.",
    detailedExplanation: "Batching amortizes fixed overheads (kernel launches, weight loading) across multiple requests. Static batching waits for N requests before starting. Dynamic batching processes arriving requests opportunistically. Continuous batching (in-flight) is most advanced — new requests join and completed ones leave between every token generation step, maintaining near-peak GPU utilization even with variable output lengths.",
    example: "Batch size 16 typically achieves 8-10x higher throughput than batch size 1, with only ~2x latency increase.",
    formula: "Throughput ∝ batch_size (diminishing returns beyond 32-64)",
  },
  "context-window": {
    term: "Context Window",
    shortDescription: "The maximum number of tokens a model can process in a single forward pass.",
    detailedExplanation: "The context window (or sequence length) determines how much text the model can 'see' at once. Larger contexts enable document understanding, long conversations, and RAG with extensive retrieved content. However, attention computation scales quadratically with sequence length (O(n²)), and KV cache memory scales linearly. Techniques like RoPE scaling, ALiBi, and sparse attention aim to extend context windows efficiently.",
    example: "Expanding from 4K to 128K context increases KV cache memory by 32x for the same batch size.",
    formula: "Attention FLOPs ∝ batch_size × seq_len² × hidden_size",
  },
  "quantization": {
    term: "Quantization",
    shortDescription: "Reducing the numerical precision of model weights to decrease memory usage and increase inference speed.",
    detailedExplanation: "Neural network weights are typically stored in 32-bit floating point (FP32). Quantization converts these to lower precision formats like 16-bit (FP16/BF16), 8-bit integer (INT8), or 4-bit (INT4/GPTQ/AWQ). This reduces VRAM requirements and can increase throughput, but may slightly degrade output quality. Post-training quantization (PTQ) methods like GPTQ and AWQ minimize quality loss by optimizing the quantization process.",
    example: "Quantizing a 70B model from FP16 to INT4 reduces VRAM from 140GB to ~35GB, fitting on a single A100.",
    formula: "VRAM = parameters × bits_per_param / 8",
  },
  "tensor-parallelism": {
    term: "Tensor Parallelism",
    shortDescription: "Splitting individual weight matrices across multiple GPUs to fit large models.",
    detailedExplanation: "Tensor parallelism shards each layer's weights across N GPUs. During forward pass, each GPU computes a partial result, and all-gather operations combine them. This allows models larger than a single GPU's VRAM to run, but introduces communication overhead proportional to hidden_size × batch_size × sequence_length. Pipeline parallelism (an alternative) splits by layers instead.",
    example: "A 405B model requires 8× H100 GPUs in tensor parallelism at FP16.",
    formula: "Communication overhead ∝ hidden_size × seq_len × batch_size / interconnect_bandwidth",
  },
  "speculative-decoding": {
    term: "Speculative Decoding",
    shortDescription: "Using a smaller draft model to predict tokens, verified by the main model in parallel.",
    detailedExplanation: "Autoregressive generation is memory-bound (limited by memory bandwidth, not compute). Speculative decoding uses a small draft model (~10x faster) to generate K candidate tokens. The main model verifies all K tokens in a single forward pass. If all are accepted, K tokens are produced for the cost of ~1. On average, 2-3x speedups are achieved with minimal quality loss.",
    example: "Using a 1B draft model with a 70B main model achieves ~2.5x speedup in memory-bound regimes.",
    formula: "Speedup ≈ acceptance_rate × K / (1 + draft_overhead)",
  },
  "memory-bandwidth": {
    term: "Memory Bandwidth",
    shortDescription: "The rate at which data can be read from GPU memory, often the bottleneck in inference.",
    detailedExplanation: "For large models, inference is memory-bound: each token requires loading all model weights from VRAM. Memory bandwidth (GB/s) determines how fast this happens. H100 has 3.35 TB/s, while A100 has 2 TB/s. Newer GPUs like H200 increase bandwidth to reduce this bottleneck. Quantization helps by reducing the amount of data to load.",
    example: "A 70B FP16 model at 2 TB/s bandwidth achieves ~28 tok/s, limited purely by memory bandwidth.",
    formula: "Max tokens/s = bandwidth / (2 × parameters × bytes_per_param)",
  },
  "paged-attention": {
    term: "Paged Attention",
    shortDescription: "vLLM's memory-efficient attention mechanism that eliminates KV cache waste.",
    detailedExplanation: "Traditional KV cache allocates fixed-size contiguous blocks for each sequence, leading to massive internal fragmentation (up to 80% waste). Paged Attention treats KV cache like virtual memory: it allocates non-contiguous physical blocks and maps them via a block table. This enables: (1) near-zero waste, (2) efficient memory sharing between sequences, (3) dynamic batching without copying, and (4) up to 2-4x higher throughput.",
    example: "vLLM with Paged Attention serves 2-4x more concurrent requests than baseline implementations.",
    formula: "Memory waste reduced from O(seq_len²) to O(block_size)",
  },
  "moe": {
    term: "Mixture of Experts (MoE)",
    shortDescription: "Architecture using conditional computation where only a subset of parameters are active per token.",
    detailedExplanation: "MoE models replace dense feed-forward layers with sparse expert layers. Each token is routed to a small subset of experts (e.g., 2 out of 64). This allows massive parameter counts (e.g., 1.8T) while keeping active parameters manageable (e.g., 39B). Benefits include higher capacity without proportional compute cost. Challenges include load balancing, communication overhead for expert routing, and increased memory requirements for storing all expert weights.",
    example: "Mixtral 8x7B has 46.7B total parameters but only uses ~13B active parameters per token.",
    formula: "Active params = num_experts_per_token × expert_size",
  },
  "continuous-batching": {
    term: "Continuous Batching",
    shortDescription: "Dynamic scheduling that adds and removes requests between token generation steps.",
    detailedExplanation: "Unlike static batching which waits for all requests to finish before starting a new batch, continuous batching (also called iterative or in-flight batching) allows new requests to join the batch and completed requests to leave between every token generation step. This maintains higher GPU utilization throughout the inference process, especially with variable-length outputs. Implemented in vLLM, TensorRT-LLM, and SGLang.",
    example: "With variable output lengths (100-2000 tokens), continuous batching achieves 10-20x better GPU utilization than static batching.",
    formula: "GPU utilization ∝ avg(batch_size) / max(batch_size)",
  },
  "latency-p50": {
    term: "P50 Latency",
    shortDescription: "Median latency — 50% of requests are faster than this value.",
    detailedExplanation: "P50 (median) represents typical user experience. In inference, latency is dominated by the autoregressive generation loop where each token depends on the previous. Memory bandwidth, batch size, and model size all affect latency. Lower is better for interactive applications. P50 is less sensitive to outliers than P95/P99.",
    example: "Chat applications target P50 < 200ms for responsive feeling.",
    formula: "Latency per token = 1000 / tokens_per_second",
  },
  "latency-p95": {
    term: "P95 Latency",
    shortDescription: "95th percentile latency — 95% of requests complete within this time.",
    detailedExplanation: "P95 captures tail latency caused by queue buildup, batch contention, or occasional slow GPU operations. In batched inference, late-arriving requests may wait for the current batch to complete. Longer outputs also increase tail latency. Systems should be provisioned based on P95/P99 SLA targets, not P50 averages.",
    example: "Enterprise SLAs often require P95 < 500ms for critical inference endpoints.",
    formula: "P95 ≈ P50 × 1.8 for well-behaved distributions",
  },
  "concurrency": {
    term: "Max Concurrency",
    shortDescription: "Maximum number of simultaneous requests the system can support without queueing.",
    detailedExplanation: "Concurrency is limited by VRAM — each request needs KV cache space. With continuous batching, hundreds of requests can be 'in flight' but only a subset execute concurrently at any moment. Higher batch sizes increase effective concurrency but increase latency per request. Memory-efficient serving (Paged Attention, quantization) directly increases max concurrency.",
    example: "vLLM on A100 80GB can serve ~100 concurrent 7B-model requests with 4K context.",
    formula: "Max concurrency ≈ VRAM / (weights + kv_cache_per_req)",
  },
  "thermal": {
    term: "Thermal Estimate",
    shortDescription: "Estimated GPU temperature based on power draw and cooling assumptions.",
    detailedExplanation: "Higher GPU utilization increases power consumption and heat output. Data centers must account for thermal design power (TDP) when provisioning cooling. Sustained high temperatures (>83°C on NVIDIA) trigger thermal throttling, reducing clock speeds and inference performance. Liquid cooling (DLC) enables higher sustained power than air cooling.",
    example: "H100 draws 350W at full load; air-cooled systems peak around 80°C.",
    formula: "Temp ≈ ambient + (power_draw / cooling_capacity)",
  },
  "power": {
    term: "Power Usage",
    shortDescription: "Total electrical power consumed by GPUs during inference.",
    detailedExplanation: "Power scales with GPU utilization and clock speeds. At idle, GPUs consume 20-50W. At full load, H100 draws 350W. Multi-GPU deployments can consume kilowatts of power. Power usage directly impacts operating costs (electricity bills) and data center cooling requirements. Efficient scheduling and right-sized deployments minimize wasted power.",
    example: "An 8×H100 server consumes ~3kW under load, costing $2,000+/month in electricity alone.",
    formula: "Power = num_GPUs × TDP × utilization_factor",
  },
  "cold-start": {
    term: "Cold Start Time",
    shortDescription: "Time from deployment trigger to first served request.",
    detailedExplanation: "Cold starts involve loading model weights from storage to GPU memory, initializing CUDA contexts, warming up kernels, and computing the first forward pass. Large models (405B) can take minutes to load from network storage. Quantized models load faster due to smaller size. Warm pools and persistent instances eliminate cold starts entirely.",
    example: "Llama-70B FP16 takes ~30s to load from NVMe, but 5+ minutes from object storage.",
    formula: "Cold start = model_load_time + first_forward_pass_overhead",
  },
  "queue-wait": {
    term: "Queue Wait Time",
    shortDescription: "Average time requests spend waiting before GPU processing begins.",
    detailedExplanation: "When incoming requests exceed max concurrency, they enter a queue. Queue wait time grows linearly with excess load. Systems with continuous batching minimize queueing by quickly slotting new requests into ongoing batches. Auto-scaling can add GPU instances when queues grow, but adds cold start latency.",
    example: "At 2x overcapacity, queue wait time ≈ average_request_latency × (load_factor - 1).",
    formula: "Queue_wait = max(0, (active_requests - max_concurrency) × avg_latency)",
  },
  "cost-hourly": {
    term: "Hourly GPU Cost",
    shortDescription: "Direct cloud rental cost per hour for the configured GPU setup.",
    detailedExplanation: "Cloud providers charge by the hour for GPU instances. On-demand pricing is highest but most flexible. Reserved instances offer 30-60% discounts for 1-3 year commitments. Spot/preemptible instances can be 70-90% cheaper but may be interrupted. On-premises has CapEx (purchase) but lower OpEx (electricity + maintenance).",
    example: "1× H100 on AWS costs ~$8.80/hr on-demand, ~$3.50/hr with 1-year reserved.",
    formula: "Hourly cost = num_GPUs × rate_per_GPU",
  },
  "cost-monthly": {
    term: "Monthly Cost",
    shortDescription: "Projected total monthly infrastructure cost including overhead.",
    detailedExplanation: "Monthly cost includes GPU rental, electricity, networking, storage, and provider overhead margins. Enterprise deployments often pay 15-30% premium over raw compute for managed services, support, and SLAs. Cost optimization strategies include quantization (smaller GPUs), batching (higher throughput per GPU), and right-sizing (matching capacity to demand).",
    example: "8× H100 serving 10M requests/day costs ~$50,000/month on AWS.",
    formula: "Monthly = hourly × 730 × overhead_multiplier",
  },
  "cost-per-request": {
    term: "Cost Per Request",
    shortDescription: "Average infrastructure cost divided by number of requests served.",
    detailedExplanation: "This shows unit economics — how much each API call costs you in infrastructure. It's highly dependent on batch efficiency and GPU utilization. Underutilized GPUs have high per-request costs. Peak efficiency is achieved when batch sizes are large and GPU utilization is consistently high (>70%).",
    example: "A well-optimized deployment achieves $0.0001/request; idle deployments can be $0.01+.",
    formula: "Cost/request = hourly_cost / requests_per_hour",
  },
  "cost-per-million-tokens": {
    term: "Cost Per Million Tokens",
    shortDescription: "Standardized pricing metric comparing across providers and configurations.",
    detailedExplanation: "The industry standard for comparing inference costs. Includes input (prompt) and output (generated) tokens. OpenAI charges $2-15/million tokens for GPT-4 depending on model. Self-hosted deployments can achieve 5-50x lower costs at scale but require engineering investment. This metric helps evaluate ROI of optimization strategies.",
    example: "Llama-3-70B on 4×A100 costs ~$0.50/million tokens self-hosted vs $0.90 via API.",
    formula: "$/M tokens = cost_per_request × 1e6 / (input_tokens + output_tokens)",
  },
  "batch-efficiency": {
    term: "Batch Efficiency",
    shortDescription: "How effectively the GPU is utilized across batched requests.",
    detailedExplanation: "Not all batch slots are productive — some requests finish early, leaving gaps. Batch efficiency measures actual GPU utilization relative to theoretical peak. Continuous batching achieves 80-95% efficiency. Static batching may drop to 30-50% with variable-length outputs. Higher efficiency = lower cost per token.",
    example: "Static batching with variable outputs: 45% efficiency. Continuous batching: 88%.",
    formula: "Efficiency = actual_throughput / (batch_size × peak_single_stream_speed)",
  },
  "model-load-time": {
    term: "Model Load Time",
    shortDescription: "Time to load model weights from storage into GPU memory.",
    detailedExplanation: "Loading happens once at startup or after scale-out. Speed depends on storage bandwidth (NVMe > SATA SSD > network) and model size. Large models require significant I/O. Pre-loading and weight sharding across GPUs can parallelize this. Quantized models load proportionally faster due to smaller size.",
    example: "Llama-3-8B FP16 (16GB) loads in 5s from NVMe, 45s from SATA SSD.",
    formula: "Load time = model_size_GB / storage_throughput_GBps",
  },
  "model-weights": {
    term: "Model Weights",
    shortDescription: "The learned parameters that define the neural network's behavior.",
    detailedExplanation: "Model weights are the numerical values (billions of them) stored from training. They dominate VRAM usage. For a model with P parameters at B bits per parameter: size = P × B / 8 bytes. Loading all weights for every token is why inference is memory-bandwidth-bound. MoE models store many more total weights but use fewer per token.",
    example: "Llama-3-70B has 70 billion parameters. At FP16 (2 bytes/param) = 140GB.",
    formula: "Size (GB) = parameters × bits_per_param / 8 / 1e9",
  },
  "activations": {
    term: "Activations",
    shortDescription: "Temporary intermediate values computed during the forward pass.",
    detailedExplanation: "Activations are the outputs of each layer during inference. They're temporary — computed, used for the next layer, then discarded (except in KV cache). Activation memory is much smaller than weights or KV cache but still significant for large batch sizes. Gradient checkpointing (training) reduces activation memory but isn't applicable to inference.",
    example: "For batch=32, hidden=4096, layers=32: activations ≈ 8GB.",
    formula: "Activations ∝ batch_size × hidden_size × seq_len × num_layers",
  },
  "fragmentation": {
    term: "Memory Fragmentation",
    shortDescription: "Unused memory gaps caused by non-contiguous allocations.",
    detailedExplanation: "GPUs allocate memory in blocks. Variable-size allocations (different sequence lengths, batch sizes) create holes. Over time, these holes accumulate even when total free memory is sufficient. Paged Attention solves this for KV cache specifically. Defragmentation or pool allocators can help, but ultimately sufficient headroom (10-20%) is needed.",
    example: "A GPU reporting 20GB free may still fail to allocate a 15GB contiguous block.",
    formula: "Fragmentation ≈ 5-10% of total allocated memory",
  },
  "cuda-graphs": {
    term: "CUDA Graphs",
    shortDescription: "Pre-recorded sequences of GPU operations that reduce CPU launch overhead.",
    detailedExplanation: "Normally, each GPU kernel launch involves CPU work (argument setup, queue insertion). CUDA Graphs record a sequence of operations once and replay it with minimal CPU involvement. Essential for low-latency inference where kernel launch time rivals execution time. Adds ~0.5GB memory overhead for graph structures.",
    example: "Enabling CUDA graphs reduces TTFT by 10-20% for small batch sizes.",
    formula: "Overhead: ~0.5GB fixed + graph_metadata",
  },
  "runtime-overhead": {
    term: "Runtime Overhead",
    shortDescription: "Memory used by inference framework, CUDA context, and libraries.",
    detailedExplanation: "Beyond model weights and KV cache, frameworks allocate workspace buffers, cuBLAS handles, CUDA streams, and other runtime structures. PyTorch allocates a caching allocator pool (typically 1-2GB). TensorRT allocates optimization profiles and engine buffers. This overhead is mostly fixed regardless of model size.",
    example: "PyTorch CUDA caching allocator reserves ~1.5GB by default per GPU context.",
    formula: "Overhead ≈ 1-3GB depending on framework",
  },
  "nvlink": {
    term: "NVLink",
    shortDescription: "High-speed direct GPU-to-GPU interconnect from NVIDIA.",
    detailedExplanation: "NVLink provides 600-900 GB/s bidirectional bandwidth between GPUs, far exceeding PCIe (32-64 GB/s). Essential for tensor parallelism where weights and activations must be exchanged between GPUs every layer. Without NVLink, multi-GPU inference becomes communication-bound. Available on A100, H100 datacenter GPUs but not consumer cards.",
    example: "Tensor parallelism on 8×A100 with NVLink: 5% overhead. Without: 40%+ overhead.",
    formula: "NVLink bandwidth: 600GB/s (NVLink 3) to 900GB/s (NVLink 4)",
  },
  "infiniband": {
    term: "InfiniBand",
    shortDescription: "High-performance networking for multi-node GPU clusters.",
    detailedExplanation: "InfiniBand provides RDMA (Remote Direct Memory Access) allowing GPUs on different servers to access each other's memory directly. Critical for distributed training and inference across nodes. Typical bandwidth: 200-400 Gbps (NVIDIA ConnectX-7). Without it, multi-node communication goes through slow TCP/IP Ethernet.",
    example: "8-node H100 cluster needs InfiniBand NDR for <5% cross-node communication overhead.",
    formula: "Bandwidth: 200Gbps (HDR) to 400Gbps (NDR)",
  },
  "pcie": {
    term: "PCIe Bandwidth",
    shortDescription: "Connection speed between CPU, GPU, and other peripherals.",
    detailedExplanation: "PCIe Gen 4 provides ~32 GB/s, Gen 5 provides ~64 GB/s. For inference, PCIe bottlenecks: (1) model loading from CPU RAM to GPU, (2) tensor parallelism without NVLink, (3) data preprocessing pipelines. Consumer GPUs (RTX 4090) use PCIe but lack NVLink, making multi-GPU tensor parallelism inefficient.",
    example: "Moving a 140GB model over PCIe 4.0 takes ~5 seconds; over PCIe 3.0 takes ~10 seconds.",
    formula: "PCIe 4.0 x16: ~32GB/s, PCIe 5.0 x16: ~64GB/s",
  },
  "parameters": {
    term: "Model Parameters",
    shortDescription: "The total number of learnable values in the neural network.",
    detailedExplanation: "Parameters are the weights and biases that the model learns during training. More parameters generally mean greater model capacity and better performance, but also higher inference cost. Dense models use all parameters for every token. MoE models have many parameters but only activate a subset per token, trading memory for conditional compute.",
    example: "GPT-4 is rumored to have 1.8T parameters (possibly MoE). Llama-3 has 8B to 405B variants.",
    formula: "For transformer: params ≈ 12 × layers × hidden_size² × (1 + vocab_size/(12×hidden_size))",
  },
  "hidden-size": {
    term: "Hidden Size",
    shortDescription: "The dimensionality of the internal representation vectors.",
    detailedExplanation: "Hidden size (also called embedding dimension or d_model) determines the width of the transformer. Larger hidden sizes allow richer representations but quadratically increase parameter count (since attention matrices are hidden×hidden). Common values: 4096 (7B models), 8192 (70B models), 16384 (405B+ models).",
    example: "Increasing hidden size from 4096 to 8192 roughly quadruples model parameters.",
    formula: "Attention params per layer = 4 × hidden_size²",
  },
  "attention-heads": {
    term: "Attention Heads",
    shortDescription: "Parallel attention mechanisms that focus on different aspects of input.",
    detailedExplanation: "Multi-head attention splits the hidden dimension into H heads, each learning different relational patterns (syntax, semantics, long-range dependencies). More heads improve expressiveness but increase compute. Head dimension = hidden_size / num_heads. Common: 32 heads for 4096 hidden (128-dim heads), 64 heads for 8192 hidden.",
    example: "32 attention heads each learn different linguistic patterns: subject-verb agreement, coreference, etc.",
    formula: "Head_dim = hidden_size / num_attention_heads",
  },
  "kv-heads": {
    term: "KV Heads (GQA)",
    shortDescription: "Grouped Query Attention — shares key/value heads across query heads.",
    detailedExplanation: "Standard multi-head attention uses separate K/V projections for each head. GQA shares K/V heads among groups of query heads, drastically reducing KV cache memory. MQA (multi-query, 1 KV head) is most extreme. GQA balances memory savings against minor quality degradation. Used in Llama-2+, Mistral, Gemma.",
    example: "Llama-3-8B: 32 query heads, 8 KV heads → 4x KV cache reduction vs standard MHA.",
    formula: "KV cache reduction = num_query_heads / num_kv_heads",
  },
  "layers": {
    term: "Layer Count",
    shortDescription: "Number of transformer blocks stacked sequentially.",
    detailedExplanation: "Each layer applies self-attention and feed-forward transformations. Deeper models (more layers) can learn more complex functions but are harder to train and slower to inference. Layer count scales linearly with compute and KV cache memory. Common ranges: 24-32 for 7B models, 80 for 70B models, 126 for 405B models.",
    example: "GPT-3 175B has 96 layers; Llama-3 8B has 32 layers.",
    formula: "Total FLOPs ∝ num_layers",
  },
  "precision-fp32": {
    term: "FP32 (Full Precision)",
    shortDescription: "32-bit floating point — highest fidelity, largest size.",
    detailedExplanation: "FP32 uses 4 bytes per parameter. Rarely used for inference due to 2× memory usage vs FP16 with negligible quality benefit. Training typically uses FP32 or mixed precision. Some legacy systems and exact reproducibility requirements may still use FP32.",
    example: "70B model in FP32 = 280GB VRAM. Virtually never deployed this way.",
    formula: "Size = params × 4 bytes",
  },
  "precision-fp16": {
    term: "FP16 (Half Precision)",
    shortDescription: "16-bit floating point — standard for inference.",
    detailedExplanation: "FP16 uses 2 bytes per parameter, halving memory vs FP32. Modern GPUs (V100+) have Tensor Cores optimized for FP16 matrix operations, delivering up to 8× throughput vs FP32. Risk of overflow/underflow in training, but generally safe for inference. De facto standard for production LLM serving.",
    example: "Llama-70B FP16 = 140GB. Fits on 2× A100 80GB with tensor parallelism.",
    formula: "Size = params × 2 bytes",
  },
  "precision-bf16": {
    term: "BF16 (Brain Float)",
    shortDescription: "16-bit with same exponent range as FP32 — more stable than FP16.",
    detailedExplanation: "BF16 trades mantissa precision for exponent range, matching FP32's dynamic range. Avoids FP16's overflow issues while maintaining the same memory savings. Preferred for training large models and inference where numerical stability matters. Supported on A100+ GPUs. Slightly slower than FP16 on some hardware.",
    example: "Training GPT-scale models almost exclusively uses BF16 or FP16 mixed precision.",
    formula: "Size = params × 2 bytes",
  },
  "precision-int8": {
    term: "INT8 Quantization",
    shortDescription: "8-bit integer weights — 4× smaller than FP32, ~1% quality loss.",
    detailedExplanation: "INT8 quantizes weights to 256 discrete levels. Requires calibration on representative data to determine optimal scaling factors. Tensor Cores on T4+ support INT8 for 2× throughput vs FP16. Quality loss is typically 1-2% on perplexity benchmarks. Good balance for production where VRAM is constrained.",
    example: "70B INT8 = 70GB. Fits on single A100 80GB. 1.5% perplexity increase.",
    formula: "Size = params × 1 byte",
  },
  "precision-int4": {
    term: "INT4 Quantization",
    shortDescription: "4-bit integer weights — 8× smaller than FP32, ~3-5% quality loss.",
    detailedExplanation: "INT4 uses only 16 discrete levels — extremely aggressive compression. GPTQ and AWQ optimize quantization groupings to minimize error accumulation. Quality loss is noticeable on complex reasoning tasks but acceptable for many applications. Enables running 70B models on consumer 24GB GPUs.",
    example: "70B INT4 = 35GB. Runs on RTX 4090 24GB with offloading. 4% quality loss.",
    formula: "Size = params × 0.5 bytes",
  },
  "fp16-tflops": {
    term: "FP16 TFLOPS",
    shortDescription: "Trillions of FP16 floating-point operations per second.",
    detailedExplanation: "Measures GPU compute capacity for half-precision matrix math. Tensor Cores accelerate these operations. High TFLOPS means faster prefill (compute-bound) and better handling of small batch sizes. Compare: H100 = 989 TFLOPS, A100 = 312 TFLOPS, RTX 4090 = 165 TFLOPS.",
    example: "H100's 989 TFLOPS processes a 512-token prefill on 70B model in ~70ms.",
    formula: "FP16 TFLOPS = tensor_core_count × clock × ops_per_cycle",
  },
  "vram-spec": {
    term: "VRAM Capacity",
    shortDescription: "Total GPU memory available for weights, cache, and buffers.",
    detailedExplanation: "VRAM is the hard capacity limit. Models must fit within available VRAM, accounting for weights, KV cache, activations, and overhead. When exceeded, frameworks either crash (OOM) or spill to CPU/system RAM (extremely slow). Multi-GPU parallelism distributes memory load. Cloud instances advertise VRAM but some is reserved by the hypervisor.",
    example: "A100 80GB: ~79GB usable. H100 80GB: ~79GB usable. RTX 4090: ~24GB usable.",
    formula: "Usable ≈ advertised - system_reserved (~1-2GB)",
  },
  "bandwidth-spec": {
    term: "Memory Bandwidth",
    shortDescription: "Peak data transfer rate between GPU memory and compute units.",
    detailedExplanation: "The defining bottleneck for LLM inference. Higher bandwidth directly increases tokens/sec for memory-bound workloads. HBM3e (H200) achieves 4.9 TB/s vs HBM3 (H100) at 3.35 TB/s vs GDDR6X (RTX 4090) at 1 TB/s. This is why datacenter GPUs outperform consumer cards despite similar TFLOPS.",
    example: "H100's 3.35 TB/s vs A100's 2 TB/s → ~67% more tokens/sec for same model.",
    formula: "Theoretical max tok/s = bandwidth / bytes_per_token",
  },
  "tensor-cores": {
    term: "Tensor Cores",
    shortDescription: "Specialized matrix-multiply accelerators in NVIDIA GPUs.",
    detailedExplanation: "Tensor Cores perform mixed-precision matrix operations (e.g., FP16 multiply + FP32 accumulate) at massively higher throughput than standard CUDA cores. Critical for transformer attention and feedforward layers. Each generation improves: Volta (V100), Ampere (A100), Hopper (H100), Blackwell (B100). AMD has similar Matrix Cores.",
    example: "A100 Tensor Cores deliver 312 TFLOPS FP16 vs 19.5 TFLOPS on CUDA cores alone.",
    formula: "Speedup: 10-20× vs CUDA cores for matrix operations",
  },
};

export const METRIC_TOOLTIP_MAP: Record<string, string> = {
  "TTFT": "ttft",
  "Tokens/s": "tokens-per-second",
  "Throughput": "throughput",
  "GPU Util": "gpu-utilization",
  "VRAM": "vram",
  "$/hr": "cost-hourly",
  "Max Concurrency": "concurrency",
  "Thermal": "thermal",
  "Queue Wait": "queue-wait",
  "Latency P50": "latency-p50",
  "Latency P95": "latency-p95",
  "KV Cache Used": "kv-cache",
  "Batch Efficiency": "batch-efficiency",
  "Power Usage": "power",
  "Model Load Time": "model-load-time",
  "Cold Start": "cold-start",
  "Hourly GPU Cost": "cost-hourly",
  "Monthly Total": "cost-monthly",
  "Per Request": "cost-per-request",
  "Per 1M Tokens": "cost-per-million-tokens",
  "Model Weights": "model-weights",
  "KV Cache": "kv-cache",
  "Activations": "activations",
  "CUDA Graphs": "cuda-graphs",
  "Fragmentation": "fragmentation",
  "Runtime": "runtime-overhead",
};